// The SSS2 control UI is a client-side app (WebSocket + live CAN state, browser-only).
// Run it as a single-page app: the adapter-node server ships the shell, the browser renders.
export const ssr = false;
export const prerender = false;
